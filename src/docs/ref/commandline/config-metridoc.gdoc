h1. config-metridoc

h2. Usage
{code}
grails config-metridoc [-o]
{code}

* o - puts the command in non interactive mode, overriding and deleting files without asking permission from the user

h2. Description

Helps setup a new plugin or application to work with MetriDoc by creating config files and deleting default views
that will conflict with the core plugin

